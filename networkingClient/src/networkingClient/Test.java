package networkingClient;

import java.net.*;
import java.io.*;

public class Test {
	public static void main(String[] args) {
		/*
		
		try {
			Socket socket = new Socket("localhost", 9090);
					
			//Thread reads from server
			ClientRead threadRead = new ClientRead(socket);
			//Thread writes to server
			ClientWrite threadWrite = new ClientWrite(socket);
			
			threadRead.start();
			threadWrite.start();
			
			
			
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
					*/

	}
}
